<h3>Nand Bame: <%= @band.name %></h3>

<form action="<%= edit_band_url(@band)%>">
  <%= auth_token %>
  <input type="submit" value="Edit">
</form>
<br>
<form action="<%= band_url(@band)%>" method = "POST">
  <%= auth_token %>
  <input type="hidden" name="_method" value="DELETE">
  <input type="submit" value="I hate this band destroy it">
</form>
<br>

Band Albums:
<% albums = @band.albums %>
<ul>
<% albums.each do |album| %>
  <li><a href="<%= album_url(album) %>"><%= album.title %></a></li>
<% end %>
</ul>
<a href="<%= new_band_albums_url(@band) %>">Add Album</a>
<br>
<br>

Back to all bands: <a href="<%= bands_url %>">All Bands</a>
